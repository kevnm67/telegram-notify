version: 2.1
description: |
  Rich Telegram notifications for CircleCI jobs: on failure (with the failed
  step's output pulled from the CircleCI API), on success, or always. Pure
  bash + curl, runs on any Linux/macOS executor, and never fails your build.
display:
  home_url: https://github.com/kevnm67/telegram-notify
  source_url: https://github.com/kevnm67/telegram-notify
